#include <iostream>

using namespace std;

int main()
{
    int a, sum = 0;

    cout << "Enter any number: ";
    cin >> a;

    for (int b = 1; b <= a; b++) 
    {
        sum += b;
    }

    cout << "The sum from the numbers of 1 to "<< a << " is " << sum;
    return 0;
}
